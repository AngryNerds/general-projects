/*var SECTION_HEIGHT = 40;
var platforms = document.getElementsByClass("platform");

for(var i=0; i<platforms.length; i++) {

    var platform = {"x":0,"y":0,"DOM":null};
    var platform.DOM = platforms[i];
    
    platform.x = 100;
    platform.y = 100;
    
    platform.DOM.style.right = platform.x + "px";
    platform.DOM.style.bottom = platform.y + "px";
}*/